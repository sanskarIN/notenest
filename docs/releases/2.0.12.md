# NoteNest 2.0.12 Release Candidate

Status: **release candidate — not yet tagged as stable**

Package version: `2.0.12+2012`

Visible application version: `2.0.12`

Pinned Flutter SDK: `3.44.7`

## Release focus

NoteNest 2.0.12 is a release-hardening milestone focused on local data integrity, deterministic async persistence, bounded local file handling, strict backup validation, collection-consistent filtering, graceful failure handling, lifecycle cleanup, accessibility, external-link resilience, and reproducible release engineering.

The release does not add a required account, cloud backend, analytics SDK, advertising SDK, or mandatory network dependency to core note-taking.

## Major engineering changes

### Deterministic editor persistence and navigation

Editor writes are serialized in submission order. Debounced autosave, lifecycle-triggered saves, export/history actions, and final navigation submissions cannot overtake each other through concurrent repository writes.

A save completion only reports `Saved locally` when the saved draft still matches the visible editor draft.

Normal editor back navigation is guarded by `PopScope`: the current visible draft is captured and saved before the route is allowed to pop. A failed save keeps the editor open and reports the problem rather than silently leaving with an unconfirmed final write.

Version-history and Markdown-export actions also require the current draft to save successfully before they begin. Initial note-load failure produces a retryable local-storage error state instead of an endless progress indicator. Version-query, restore, and export failures are contained with user-visible feedback.

Markdown-lite heading/list/checklist formatting now handles caret offset zero correctly when a note begins with a newline: formatting targets the actual empty first line instead of jumping to the second line. Widget regression coverage protects this boundary.

A background/lifecycle save is still queued for non-navigation lifecycle transitions and forced disposal paths; real-device process-termination behavior remains a release smoke-test requirement.

### Reliable note-browser mutations

The notes browser and HomeShell now contain local-storage failures around create/open/pin/favorite/archive/trash/restore/permanent-delete/empty-trash/undo actions. Instead of leaking asynchronous exceptions through UI callbacks, the app reports concise failure messages and preserves existing data where the failed operation did not complete.

### Reliable settings, onboarding, and root lifecycle persistence

Settings persistence has an injectable `SettingsStore` boundary and ordered mutation queue.

For theme, text size, reduced motion, and app-lock preference changes:

- Visible state may update optimistically.
- Writes are serialized.
- A failed write propagates as a storage failure.
- If the failed value is still current, the controller restores the last successfully persisted value.
- A stale failed write does not replace a newer requested visible value.
- Reported `shared_preferences` setter failure is not silently treated as success.

Onboarding uses a stricter persistence-first rule: completion is written successfully before the application transitions away from onboarding. A failed write leaves onboarding visible and retryable with a user-facing error.

Application dependency creation cleans up the partially created settings controller/database if initial settings loading fails before the startup fallback screen is shown. The root `NoteNestApp` now also invokes `AppDependencies.dispose()` when its state is permanently removed, so the settings controller and Drift database follow the same explicit ownership/cleanup boundary during normal application teardown.

### Safer local imports

Markdown/text and NoteNest JSON backup imports do not request eager picker byte loading on native platforms.

NoteNest receives the picker's cached local path and reads it through `BoundedFileReader`:

- Markdown/text ceiling: 16 MiB.
- JSON backup ceiling: 64 MiB.
- Reported file length is checked before streaming.
- Every streamed chunk re-checks the active limit before being added to the final buffer.
- Filesystem failures are translated to import/export errors.
- UTF-8 decoding occurs only after the bounded read.
- Backup JSON validation occurs only after decoding.

The platform picker/provider may still perform its own caching before returning the local path, so real-platform oversized-file testing remains required.

### Stricter backup/restore validation

Restore validation includes:

- NoteNest application identity.
- Backup schema version.
- Valid explicit-UTC root `exportedAt`.
- Required value types.
- Serialized tag JSON, canonicalized to NoteNest trim/deduplicate/sort rules.
- Duplicate incoming note IDs.
- No surrounding whitespace in imported note/version identifiers.
- Version-to-note relationships.
- `colorValue` null or within the 32-bit ARGB integer range.
- Explicit UTC note/version timestamps ending in `Z`.
- `updatedAt` must not precede `createdAt` for a note.
- Impossible lifecycle states such as archived+trashed or pinned+trashed are rejected.

Validation completes before restore transaction writes. Restore remains transactional and preserves newer local note revisions.

### Collection-consistent filters

Folder/tag metadata uses the same lifecycle predicate as the active collection:

- All Notes excludes Archive/Trash metadata.
- Favorites exposes only active favorite metadata.
- Archive exposes archived metadata.
- Trash exposes trashed metadata.

Switching collections clears stale folder/tag selections. This fixes the earlier behavior where Trash could contain folders/tags that were impossible to select and other collections could advertise irrelevant options.

### Cross-platform and Unicode-safe export filenames

Markdown exports:

- Normalize invalid/control filename characters.
- Trim prohibited trailing dots/spaces.
- Protect Windows reserved device names.
- Preserve Unicode.
- Limit generated basenames.
- Fall back safely for unusable titles.
- Truncate by Unicode code points rather than splitting a UTF-16 surrogate pair at the limit boundary.

### External-link resilience

Repository, funding, business-email, support-email, and release links use a centralized `ExternalLinkService`.

Launcher refusal/exception returns a controlled failure result instead of escaping into feature code. About and Settings provide user-visible feedback.

### Accessibility

The custom note-color selector uses:

- A 48 logical-pixel interaction target.
- Explicit selected semantics.
- A visible checkmark for selection.
- A non-color reset cue for the default option.

Collection empty states are context-specific so Favorites, Archive, and Trash do not offer creation/import actions whose result would be hidden by the active collection.

### Reproducible toolchain and release metadata

Flutter `3.44.7` is pinned consistently in:

- `.flutter-version`.
- CI quality workflow.
- Native platform build workflow.
- Release artifact workflow.

`tool/check_version_sync.py` verifies:

- `pubspec.yaml` package semantic version/build number.
- Matching `AppStrings.version`.
- Matching `CHANGELOG.md` release section.
- Matching `docs/releases/<version>.md` containing exact package/visible version values.
- `.flutter-version` contains a valid exact SDK version.
- Every Flutter action pin in the CI, native platform-build, and release workflows matches `.flutter-version`.

CI runs this before dependency resolution.

Native platform-build verification also triggers for bundled `assets/**` changes so a branding/asset-only pull request cannot bypass compile verification.

`tool/bootstrap_platforms.py` now verifies each required native patch after generation. If a future Flutter template no longer matches the expected Android `FlutterFragmentActivity`, minimum-SDK, AppCompat dependency/theme, biometric permission, or iOS Face ID configuration paths, bootstrap fails loudly instead of reporting a misleading success.

`tool/check_repo.py` now requires the complete release/documentation/automation baseline, including `.flutter-version`, `build.yaml`, canonical assets, GitHub guidance, platform/release/security workflows, issue-template configuration, native bootstrap tooling, and the repository security scanner.

## New/updated quality tools

- `tool/check_repo.py`
- `tool/check_repository_reference.py`
- `tool/check_markdown_links.py`
- `tool/check_version_sync.py`
- `tool/security_scan.py`
- `tool/bootstrap_platforms.py`

The CI quality gate performs release/toolchain version synchronization, dependency/code generation, formatting, analyzer, tests, repository policy, exhaustive tracked-file documentation coverage, local documentation-link checks, and the lightweight secret scan.

## Regression coverage added during hardening

### Core/services

- Markdown-lite behavior.
- Markdown document metadata codec.
- Logger redaction.
- Import byte limits.
- Bounded native-file reads.
- Cross-platform/Unicode-safe filenames.
- Async serial task ordering.
- External-link success/refusal/exception behavior.

### Application/features

- Settings atomic load, write rollback, rapid write ordering, onboarding persistence-first behavior, and app-lock rollback.
- Notes collection-switch filter reset and collection-specific metadata loading.

### Data

- Note repository lifecycle/search/snapshot behavior.
- Collection-scoped folder/tag metadata, including Trash.
- Backup application/schema/export-time/tag/ID/relationship validation.
- Backup lifecycle-state validation.
- Backup UTC and timestamp-order validation.
- Backup 32-bit ARGB color validation.
- Backup tag normalization.
- Backup newer-local conflict handling.
- Settings repository default/successful persistence behavior.

### Widgets

- Onboarding success/failure feedback.
- Collection-specific empty actions.
- Note-color accessibility cues/target size.
- Editor missing-note load error and save-before-back behavior.
- Editor caret-offset-zero formatting when the note starts with an empty first line.
- About external-link failure feedback.

## Data compatibility

The current Drift database schema remains **schema version 1**. Application version 2.0.12 does not imply database schema version 2.

The 2.0.12 hardening does not intentionally introduce a destructive database migration. Existing local notes should remain compatible with the current schema. Backup schema compatibility remains independently versioned by the backup format.

A stable release must still test upgrades/opening representative fictional existing data on real supported platforms.

## Privacy/security behavior

Core note content remains local-first. NoteNest does not claim that the SQLite database is encrypted at rest. Optional app lock uses platform authentication and should not be described as database encryption.

Imported files are untrusted input. Preference storage is for non-sensitive settings, not credentials/note content. External links open only after user action. No signing secret, API credential, or real user note database belongs in the repository.

See:

- [`../../SECURITY.md`](../../SECURITY.md)
- [`../../PRIVACY.md`](../../PRIVACY.md)

## Required verification before stable tag

### Repository/toolchain

- [ ] `python tool/check_version_sync.py`
- [ ] `python tool/check_repo.py`
- [ ] `python tool/check_repository_reference.py`
- [ ] `python tool/check_markdown_links.py`
- [ ] `python tool/security_scan.py`
- [ ] `dart run build_runner build --delete-conflicting-outputs`
- [ ] `dart format --output=none --set-exit-if-changed lib test`
- [ ] `flutter analyze`
- [ ] `flutter test --coverage`

### Native builds

- [ ] Android release compile/build verification.
- [ ] Windows release compile verification.
- [ ] Linux release compile verification.
- [ ] macOS release compile verification.
- [ ] iOS no-codesign compile verification.

### Manual runtime/data checks

- [ ] Rapid-edit → immediate Back → newest draft saved before pop.
- [ ] First-line Markdown toolbar formatting at caret offset zero when the document begins with a newline.
- [ ] Simulated save failure → editor remains open; export/history do not continue.
- [ ] Missing/deleted note editor load → retryable error state.
- [ ] Note-browser mutation/storage failure feedback.
- [ ] Rapid settings changes persist in request order.
- [ ] Simulated/real preference failure produces rollback/retry-safe behavior where testable.
- [ ] Onboarding successful persistence and failed-persistence retry behavior.
- [ ] Collection folder/tag filters for All Notes/Favorites/Archive/Trash.
- [ ] Markdown import/export smoke test including Unicode/Windows-reserved titles.
- [ ] Oversized Markdown/text rejection through a real picker/provider.
- [ ] Backup export/restore smoke test using fictional data.
- [ ] Malformed export timestamp/UTC/color/ID/lifecycle backup rejection.
- [ ] Oversized backup rejection through a real picker/provider.
- [ ] External repository/funding/mail/release link success/failure tests on representative platforms.
- [ ] App-lock supported and unsupported flows.
- [ ] Startup fallback after a simulated dependency/settings initialization failure where testable.
- [ ] Root app teardown closes owned dependencies without disposal errors in a controlled lifecycle test where available.

### Accessibility/release presentation

- [ ] Desktop keyboard traversal.
- [ ] Mobile screen-reader review.
- [ ] Large-text/light-dark/reduced-motion review.
- [ ] Note-color selection semantics/visual cue review.
- [ ] Failure SnackBars/messages readable with accessibility settings.
- [ ] Verified runtime screenshots.
- [ ] Final artifact signing status and checksums documented.
