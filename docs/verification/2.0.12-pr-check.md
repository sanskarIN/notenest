# NoteNest 2.0.12 PR Verification Trigger

This verification-only file creates a pull-request checkpoint for the 2.0.12 release candidate so PR-triggered GitHub Actions results can be inspected before a stable tag is considered.

It does not change runtime behavior. The release remains blocked until required CI/native/manual checks are recorded as successful.
